# application name is property_source in spring cloud server
# config will be fetched from http://server:port/{spring.application.name}/{environment}
spring.application.name=limits-service
# spring profile or environment
spring.profiles.active=qa
# spring cloud server URL
spring.cloud.config.uri=http://localhost:8888
